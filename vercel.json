{
  "buildCommand": "npm run build",
  "installCommand": "npm install && python -m pip install -r backend/requirements.txt",
  "outputDirectory": ".next",
  "env": {
    "PYTHONUNBUFFERED": "1",
    "PYTHONPATH": "/var/task"
  }
}
